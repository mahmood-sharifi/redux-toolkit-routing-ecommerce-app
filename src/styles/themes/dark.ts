import { colors } from '../variables';

export const darkTheme = {
  primaryColor: colors.backgroundDark,
  primaryHoverColor: colors.primaryHover,
  backgroundColor: colors.backgroundDark,
  textColor: colors.textLight,
};
